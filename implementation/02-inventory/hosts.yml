---
all:
  children:
    ocpvirt_vms:
      hosts:
        rhel9-app-01:
          ansible_host: 203.0.113.10   # Cluster B node IP
          ansible_port: 30022          # NodePort -> rhel9-app-01 SSH (see 00-prerequisites.md)
        rhel9-db-01:
          ansible_host: 203.0.113.10   # Cluster B node IP
          ansible_port: 30122          # NodePort -> rhel9-db-01 SSH

    # NOT EXECUTED - reference only. No VMware infrastructure available
    # in this environment. See ../00-prerequisites.md.
    vmware_vms:
      hosts:
        vmw-app-01:
          ansible_host: 198.51.100.20  # vCenter-managed VM, reachable directly
          ansible_port: 22
